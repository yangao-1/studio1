/// <reference types="node" />
import 'array-flat-polyfill';
export type { Buffer } from 'buffer';
export type Inflates = {
    inflateAsync: (b: Buffer) => Buffer | Promise<Buffer>;
    brotliDecompressAsync: (b: Buffer) => Buffer | Promise<Buffer>;
    Buffer: typeof Buffer;
};
export declare const makeDecoder: ({ inflateAsync, brotliDecompressAsync }: Inflates) => (buffer: Buffer) => Promise<{
    buf: Buffer;
    type: string;
    protocol: number;
    data: any;
}[]>;
type EncodeType = 'heartbeat' | 'join';
export declare const encoder: (type: EncodeType, { Buffer }: Inflates, body?: any) => Buffer;
