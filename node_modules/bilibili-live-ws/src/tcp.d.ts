/// <reference types="node" />
/// <reference types="node" />
import { Socket } from 'net';
import { Inflates } from './buffer';
import { LiveOptions, Live } from './common';
export type TCPOptions = LiveOptions & {
    host?: string;
    port?: number;
};
export declare class LiveTCPBase extends Live {
    socket: Socket;
    buffer: Buffer;
    i: number;
    constructor(inflates: Inflates, roomid: number, { host, port, ...options }?: TCPOptions);
    splitBuffer(): void;
}
